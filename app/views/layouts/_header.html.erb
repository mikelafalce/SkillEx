
<nav style="position: fixed;
   top: 0px; background-color: transparent; border-color: transparent;
" class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a style="margin-top:8px; color:#ffa31a;" class="navbar-brand" href="/">SkillEx</a>
      <%= form_tag skills_path, method: :get do %>
        <%= text_field_tag :search, params[:search] %>
        <%= submit_tag "Search", name: nil %>
      <% end %>
    </div>


    <ul class='nav navbar-nav navbar-right'>
        <% if current_user && current_user.avatar.url %>
          <%= link_to(image_tag(current_user.avatar.mini.url), edit_user_registration_path) %>
        <%else%>
        
        <%end%>

        <% if current_user %>
        <li style="margin-top:8px; margin-right:450px; color:#ffa31a;">You have: <%= current_user.points %> <%= current_user.points == 1 ? 'Point' : 'Points' %></li>
          <li><%= link_to 'All Skills', skills_path, {style: "color: #ffa31a"} %></li>
          <li><%= link_to 'My Skills', my_skills_path, {style: "color: #ffa31a"} %></li>
          <li><%= link_to 'My Upcoming Lessons', upcoming_lessons_path, {style: "color: #ffa31a"} %></li>
          <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li><br>
        <% else %>
          
        <% end %>

        

     
    </ul>
  </div>
</nav>
