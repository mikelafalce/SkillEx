  <%= form_for(skill) do |f| %>
  <% if skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %><br />

  <div class="field">
   <%= f.label :title %><br />
   <%= f.text_field :title %>
  </div><br /><br />

  <div>
    <label for="description">Description</label><br />
    <textarea rows="4" cols="50" name="skill[description]" id="description" value="<%= skill.description %>" required ></textarea>
  </div><br />

  <div  class="field">
    <%= f.label :skill_picture %>
    <%= f.file_field :avatar %>
  </div>
  <br />

  <div align="center" class="actions">
    <%= f.submit :class => "request-chat btn" %>
  </div>
<% end %>
