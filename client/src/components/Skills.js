import React, { Component } from 'react';

class Skills extends Component {
  render() {
    const { date, filterText } = this.props;

    const skillsList = data
      .filter(title => {
        return
      })
      .map(title => {

      })
    return (
      <div></div>
      );
  }
}